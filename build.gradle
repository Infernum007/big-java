allprojects {
    group 'tutorials'
    version '1.0-SNAPSHOT'
}

subprojects {
    if (name == 'early-objects' || name == 'late-objects') {
        return
    }

    apply plugin: 'java'

    repositories {
        jcenter()
    }

    dependencies {
        testCompile 'junit:junit:4.12'
    }
}
